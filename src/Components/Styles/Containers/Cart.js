import styled from 'styled-components';

const CartContainer = styled.div`
    position: absolute;
    right: 5%;
    top:10%;

    h4{
        font-weight: 700;
        font-size: 16px;
        line-height: 160%;
        text-align: right;
        color: #1D1F22;
    }
`;

export default CartContainer;
